Link to the project:
https://codepen.io/shabginov/pen/VwYEwvL?editors=1010

1. What is Semantic HTML?
Semantic HTML means providing meaning to a web page

2. What is HTML used for?
HTML is used as a carcass for a web page

3. What is an attribute and where do we put it?
Attribute provides additional information about HTML elements

4. What is the h1 tag used for? How many times should I use it on a page?
h1 tag is the biggest of all h# tags (there are total six tags) 

5. Name two tags that have required attributes?
Tag <a> and <img>

6. What do we put in the head of our HTML document?
Tags like title, style, meta, link, script and base

7. What is an id?
id attribute specifies a unique id for an HTML element

8. What elements can I add an id to?
You can add an id to tags <h#>, <div>, <img>

9. How many times can I use the same id on a page?
Only once per a page

10. What is a class?
.class selector selects elements with a specific class attribute

11. What elements can I add a class to?
The class attribute can be used on any HTML element

12. How many times can I use the same class on a page? 
Multiple times

13. How do I get my link to open in a new tab?
By adding target='_blank' attribute to your links

14. What is the alt attribute used for?
It provides alternative information for an image if a user can't view it

15. How do I reference an id?
By #

16. What is the difference between a section and a div
Section element represents a generic section of a document. Div element has no special meaning

17. What is CSS used for?
CSS is used for styling web pages

18. How to we select an element? Example - every h2 on the page
h2 {
    attribute: someValue;
}

19. What is the difference between a class and an id? - Give me an example of when I might use each one

20. How do we select classes in CSS?
By .

21. How do we select a p element with a single class of “human””?
.human p {

}

22. What is a parent child selector? When would this be useful?
To inherit a style from a parent to a child element

23. How do you select all links within a div with the class of sidebar?
div.sidebar a{
}

24. What is a pseudo selector?
Pseudo-selector is a keyword added to a selector that specifies a special state of the selected elements

25. What do we use to change the spacing between lines?
By using line-height property

26. What do we use to change the spacing between letters?
By using letter-spacing property

27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
By using text-transform property with values uppercase or lowercase or capitalize;

28. How do I add a 1px border around my div that is dotted and black?
div {
border: 1px, dotted, black;
}

29. How do I select everything on the page?
By using global selector * {
}

30. How do I write a comment in CSS?
/* some comment */

31. How do I find out what file I am in, when I am using the command line? 
Use command pwd

32. Using the command line - how do I see a list of files/folders in my current folder?
Use command ls 

33. How do I remove a file via the command line? Why do I have to be careful with this?
By running command rm. The command deletes files irreversibly

34. Why should I use version control?
Version control allows people to track changes of the files they are working on

35. How often should I commit to github?
It is recommended to commit whenever a user has finished working on something

36. What is the command we would use to push our repo up to github?
git push

37. Walk me through Lambda's git flow.
- Student forks a repo
- Student adds a TL as a collaborator
- Student clones the repo to their local machine
- Student creates a new named branch and switches to it
- Student works on a project committing often
- Student pushes changes to GitHub
- Student creates a pull-request for the TL
